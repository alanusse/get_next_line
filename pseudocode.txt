VARIABLE buffer
VARIABLE list
VARIABLE read_bytes

read_bytes = read(fd, buffer, BUFFER_SIZE)
WHILE read_bytes > 0 OR buffer not contains new line
    IF list is not created
        create list with buffer content
    ELSE
        read_bytes = read(fd, buffer, BUFFER_SIZE)
        create new list with new buffer content